{
  "$schema": "https://antigravity.google/schemas/v1/plugin.json",
  "name": "tts",
  "description": "Speaks agent responses aloud via the shared agent-tts daemon (macOS/Windows/Linux, optional cloud voices). Opt-in; Stop hook is fail-open and never continues the agent loop."
}
